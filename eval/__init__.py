"""Scoring the reader against real labels.

`read_accuracy` reads the 30 TTB-approved labels under
`tests/fixtures/labels/` with one of the two readers and reports, check by
check, how often it returned the value the label really carries.
"""
